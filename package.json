{
  "name": "hls-token-service",
  "version": "1.0.0",
  "description": "Token extraction service using Puppeteer for HLS streams",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "puppeteer": "^24.1.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
